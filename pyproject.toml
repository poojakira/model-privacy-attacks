[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "model-privacy-attacks"
version = "0.2.0"
description = "Null-calibrated privacy certification + offline simulator for membership inference and model extraction attacks."
readme = "README.md"
requires-python = ">=3.10"
license = { text = "MIT" }
authors = [{ name = "poojakira" }]
keywords = [
    "membership-inference",
    "model-extraction",
    "privacy",
    "machine-learning-security",
]
dependencies = [
    "scikit-learn>=1.5",
    "numpy>=1.26",
    "pyyaml>=6.0",
]

[project.optional-dependencies]
dev = [
    "pytest>=8.2",
    "pytest-cov>=5.0",
]

[project.scripts]
privacy-certify = "privacy_attacks.certification.cli:main"

[tool.hatch.build.targets.wheel]
packages = ["src/privacy_attacks"]

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-q"
pythonpath = ["src"]
